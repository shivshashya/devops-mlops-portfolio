{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
	"scripts": {
		"dev": "astro dev",
		"build": "astro build && node scripts/prepare-sites-deploy.mjs",
		"build:cloudflare": "astro build",
		"preview:cloudflare": "npm run build:cloudflare && wrangler dev",
		"deploy:cloudflare": "npm run build:cloudflare && wrangler deploy",
		"preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.1",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2",
    "resend": "^6.26.0"
  },
  "allowScripts": {
    "esbuild": true,
    "workerd@1.20260907.1": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "wrangler": "^4.129.1"
  }
}
